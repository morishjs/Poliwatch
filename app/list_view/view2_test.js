'use strict';

describe('myApp.list_view module', function() {

  beforeEach(module('myApp.list_view'));

  describe('list_view controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});